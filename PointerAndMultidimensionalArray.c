#include<stdio.h>
#include<stdlib.h>
int main(){
/*For 2_D array B[][], we can write as B[i][j]=*(B[i]+j) or =*(*(B+i)+j). These all three expressions point to the same thing . We can write *(A+i)=A[i] and (A+i) as &A[i]. *B=B[0] so *(*B+1)=(B[0]+1)=B[0][1]*/

}